# Cấu hình deploy cho Netlify — site tĩnh, không cần build.
[build]
  publish = "."

# Bảo đảm mọi trang trả về đúng file; SPA của app tự lo điều hướng nội bộ.
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
